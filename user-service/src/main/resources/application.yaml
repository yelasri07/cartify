spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: ${MONGO_URL:mongodb://buy01:buy01@localhost:27017/buy01?authSource=admin}

server:
  port: 8081
  servlet:
    context-path: /api

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}