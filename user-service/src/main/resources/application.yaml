spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: mongodb://${DB_USERNAME:buy01}:${DB_PASSWORD:buy01}@localhost:27017/${DB_NAME:buy01}?authSource=admin

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}